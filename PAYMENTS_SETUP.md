# Пошаговая настройка платежей в CareerAI Bot

Платежи в боте работают через **Telegram Payments**: пользователь нажимает «Купить Premium», получает счёт прямо в Telegram и оплачивает картой или через выбранный провайдер. Деньги приходят на счёт провайдера (Stripe, YooKassa и т.д.), а бот только получает уведомление об успешной оплате и выдаёт премиум.

---

## Шаг 1. Выбор платёжного провайдера

Нужно выбрать **одного** провайдера и подключить его в BotFather.

| Провайдер   | Регион      | Валюта | Комиссия (примерно) |
|------------|-------------|--------|---------------------|
| **Stripe** | Международный | USD, EUR и др. | ~2.9% + 30¢ |
| **YooKassa** | РФ, СНГ   | RUB    | ~2.8% |
| **SberPay** | РФ         | RUB    | по тарифам банка |

- Если целевая аудитория в **России** — удобнее **YooKassa** (рубли, российские карты).
- Если аудитория **международная** — **Stripe** (доллары/евро, карты мира).

Дальше в инструкции — два варианта: **YooKassa** и **Stripe**. Выбери один и выполни только его шаги.

---

## Шаг 2А. Настройка YooKassa (для РФ)

### 2А.1. Регистрация в YooKassa

1. Зайди на [yookassa.ru](https://yookassa.ru).
2. Нажми **«Подключиться»** и зарегистрируйся (ИП или ООО для приёма платежей; для теста можно личный кабинет — проверь актуальные условия на сайте).
3. В личном кабинете открой **«Настройки» → «Способы приёма платежей»** и включи приём платежей.
4. В разделе **«Интеграция»** найди:
   - **shopId** (идентификатор магазина),
   - **Секретный ключ** (secret key).

Их не вводим в бота напрямую: для Telegram нужен **токен от BotFather**, который привязан к YooKassa.

### 2А.2. Подключение YooKassa к боту в BotFather

1. Открой Telegram и найди **@BotFather**.
2. Отправь команду: **`/mybots`**.
3. Выбери своего бота (CareerAI).
4. Нажми **«Bot Settings»** (Настройки бота).
5. Нажми **«Payments»** (Платежи).
6. Выбери **«YooKassa»** (или «YooMoney» — как в списке).
7. BotFather попросит ввести **Token** (секретный ключ из личного кабинета YooKassa). Вставь **Секретный ключ** из шага 2А.1.
8. После успешной привязки BotFather пришлёт сообщение вида:  
   **«Provider token: 381764678:TEST:...»** или просто подтверждение.

Этот **Provider token** (токен провайдера) и есть то, что нужно боту. Его мы вставим в переменные окружения на шаге 4.

---

## Шаг 2Б. Настройка Stripe (международные платежи)

### 2Б.1. Регистрация в Stripe

1. Зайди на [stripe.com](https://stripe.com) и зарегистрируйся.
2. В панели Stripe открой **Developers → API keys**.
3. Будут два ключа:
   - **Publishable key** (pk_...) — не нужен для Telegram Payments,
   - **Secret key** (sk_...) — это и есть токен для BotFather.

Для теста используй **Test mode** (переключатель вверху) и **Secret key** из тестового режима (sk_test_...).

### 2Б.2. Подключение Stripe к боту в BotFather

1. Открой **@BotFather** в Telegram.
2. **`/mybots`** → выбери бота → **Bot Settings** → **Payments**.
3. Выбери **«Stripe»**.
4. BotFather попросит ввести **API Token**. Вставь **Secret key** из Stripe (sk_test_... для теста или sk_live_... для боевого режима).
5. BotFather пришлёт подтверждение и может показать **Provider token**. Если не показал — токен уже привязан к боту; используй тот же Secret key (sk_...) как значение для переменной окружения (см. шаг 4).

Для Stripe в боте в переменных окружения обычно указывают тот же **Secret key** (sk_...), который вводили в BotFather.

---

## Шаг 3. Что такое Provider Token и где его взять

- **Provider token** — это ключ, который BotFather связывает с твоим ботом и выбранным провайдером (YooKassa или Stripe).
- Для **YooKassa**: после привязки BotFather может прислать строку вида `381764678:TEST:...` — это и есть Provider token. Используй её.
- Для **Stripe**: часто используют тот же **Secret key** (sk_test_... или sk_live_...), который вводили в BotFather. То есть в переменную окружения `PAYMENT_PROVIDER_TOKEN` вставляешь этот Secret key.

Итог: в коде и в Vercel везде используется одна и та же строка — либо выданный BotFather Provider token (YooKassa), либо Stripe Secret key (sk_...).

---

## Шаг 4. Переменные окружения в проекте

В корне проекта в файле **`.env`** (и затем в Vercel) нужно задать:

```env
# Обязательно: токен от BotFather (Payments). Для Stripe — Secret key (sk_...), для YooKassa — выданный BotFather токен.
PAYMENT_PROVIDER_TOKEN=sk_test_xxxxxxxxxxxxxxxxxxxxxxxx

# Необязательно: по умолчанию 999 = 9.99 USD (центы). Для рублей задать сумму в копейках, например 99900 = 999 ₽.
PREMIUM_PRICE_CENTS=999

# Необязательно: валюта — USD или RUB
PREMIUM_CURRENCY=USD

# Необязательно: срок подписки в днях (по умолчанию 30)
PREMIUM_DAYS=30
```

Пример для РФ (рубли):

```env
PAYMENT_PROVIDER_TOKEN=381764678:TEST:xxxxxxxxxxxx
PREMIUM_PRICE_CENTS=99900
PREMIUM_CURRENCY=RUB
PREMIUM_DAYS=30
```

(99900 копеек = 999 ₽.)

**Важно:** в `.env` не коммить реальные ключи в git. Файл `.env` уже в `.gitignore`. На Vercel эти же переменные нужно добавить в **Settings → Environment Variables** (см. шаг 5).

---

## Шаг 5. Добавление переменных на Vercel

1. Зайди на [vercel.com](https://vercel.com) и открой свой проект (CareerAI Bot).
2. Вверху открой **Settings** → **Environment Variables**.
3. Добавь переменные (для **Production** и при необходимости для Preview):

| Name                     | Value                    | Пример                    |
|--------------------------|--------------------------|---------------------------|
| `PAYMENT_PROVIDER_TOKEN` | Токен из шага 3          | `sk_test_...` или `381764678:TEST:...` |
| `PREMIUM_PRICE_CENTS`    | Число (центы/копейки)    | `999` или `99900`         |
| `PREMIUM_CURRENCY`       | USD или RUB              | `USD` или `RUB`           |
| `PREMIUM_DAYS`           | Число дней подписки      | `30`                      |

4. Сохрани изменения и сделай **Redeploy** последнего деплоя (Deployments → три точки у деплоя → Redeploy).

После редеплоя бот начнёт использовать новые переменные.

---

## Шаг 6. Проверка в боте

1. Открой бота в Telegram.
2. Отправь **`/premium`** или нажми кнопку **«💎 Премиум»** в меню.
3. Должна появиться кнопка **«💳 Купить Premium»**.
4. Нажми её — в чат должен прийти **счёт** (invoice) от Telegram.
5. В тестовом режиме (Stripe Test / YooKassa тест) оплати тестовой картой — после успешной оплаты бот должен написать, что премиум активирован, и в **`/stats`** отобразить «Премиум активен» и «∞ (Премиум)» по запросам.

Если счёт не приходит — проверь, что `PAYMENT_PROVIDER_TOKEN` действительно записан в Vercel и редеплой выполнен. Если счёт приходит, но после оплаты премиум не включается — пришли текст ошибки из логов Vercel (Functions → логи).

---

## Краткий чеклист

- [ ] Выбран провайдер (YooKassa или Stripe).
- [ ] Зарегистрирован в личном кабинете провайдера.
- [ ] В @BotFather: Payments → выбран провайдер → введён токен/ключ.
- [ ] В `.env` и в Vercel заданы `PAYMENT_PROVIDER_TOKEN`, при необходимости `PREMIUM_PRICE_CENTS`, `PREMIUM_CURRENCY`, `PREMIUM_DAYS`.
- [ ] Сделан редеплой на Vercel.
- [ ] В боте есть кнопка «Купить Premium», при нажатии приходит счёт, после тестовой оплаты премиум активируется.

---

## Частые проблемы

**Кнопка «Купить Premium» не появляется**  
- Не задан `PAYMENT_PROVIDER_TOKEN` в Vercel или он пустой.  
- Не сделан редеплой после добавления переменных.

**Счёт не приходит / ошибка при отправке счёта**  
- **YooKassa:** в `PAYMENT_PROVIDER_TOKEN` должен быть токен **от BotFather** (после Payments → YooKassa → ввод секретного ключа из ЛК YooKassa). BotFather присылает строку вида `381764678:TEST:...` — подставляй именно её. Секретный ключ из личного кабинета YooKassa в переменную **не** вставлять.  
- Для YooKassa валюта обычно **RUB** (`PREMIUM_CURRENCY=RUB`), сумма — в копейках (например `99900` = 999 ₽).  
- **Stripe:** в продакшене нужен sk_live_..., в тесте — sk_test_...  
- После нажатия «Купить Premium» бот теперь присылает в чат текст ошибки от Telegram — по нему можно понять причину (например PROVIDER_TOKEN_INVALID).  
- Логи: Vercel → Functions → api/index.py → Logs.

**После оплаты премиум не активируется**  
- Проверь логи на ошибки в обработчике `successful_payment`.  
- Убедись, что в BotFather подключён тот же провайдер и тот же токен, что и в `PAYMENT_PROVIDER_TOKEN`.

Если опишешь, на каком шаге застрял и что видишь в боте/логах, можно разобрать точечно.
